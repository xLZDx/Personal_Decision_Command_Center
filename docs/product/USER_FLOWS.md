# User Flows

Canonical source: `docs/architecture/TDD.md` §49 (product dashboard), §2 (product goals),
§30-31 (priority engine, proactive alerts). To be expanded with concrete screen-by-screen flows at
G7 planning — this file is a placeholder pointing at the primary surfaces until then.

## Primary surfaces (TDD §49)

```
TODAY
DECISIONS
STREAMS
TOPICS / INTENTS
KNOWLEDGE
SEARCH
```

`TODAY` shows prioritized structured actions/decisions, not per-channel unread counts:

```
P1  ERP / Gate 4.2
    Decision required
    Evidence: Telegram + Gmail
    Updated 4m ago
    [OPEN] [SNOOZE]
```

Navigation: `Project -> Stream -> Topic/Intent -> Knowledge -> Decision -> Evidence`.

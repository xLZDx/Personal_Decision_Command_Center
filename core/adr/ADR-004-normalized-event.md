# ADR-004: Normalized Event Contract

**Status:** NOT YET REQUIRED for G0 closure — placeholder per `docs/architecture/TDD.md` §55's
repository tree. To be written and adopted no later than G2 planning (D1 schema/ingest gate),
drawing on TDD §13 (normalized event contract JSON shape) and §7 (provenance wrapper for any
content-derived `routing_hints` entry — MINOR finding MIN-1 from the v0.2 review).
